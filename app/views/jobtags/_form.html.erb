<%= form_for(@jobtag) do |f| %>
  <% if @jobtag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@jobtag.errors.count, "error") %> prohibited this jobtag from being saved:</h2>

      <ul>
      <% @jobtag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :job_id %><br>
    <%= f.text_field :job_id %>
  </div>
  <div class="field">
    <%= f.label :tag_id %><br>
    <%= f.text_field :tag_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
